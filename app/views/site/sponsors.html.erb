<h1>Sponsors</h1>
<div class="row mx-5">
	<% @sponsors.each do |sponsor| %>
		<div class="col-md-2 col-sm-6 mb-4">
			<b><p class="test-text"><%= sponsor.title %><p></b>
			<%= link_to image_tag(sponsor.image, class:"w-100"), sponsor.website, target:"_blank" %>
		</div>
	<% end %>
</div>